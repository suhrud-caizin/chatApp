import { CurrentuserDirective } from './currentuser.directive';

describe('CurrentuserDirective', () => {
  it('should create an instance', () => {
    const directive = new CurrentuserDirective();
    expect(directive).toBeTruthy();
  });
});
